FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}

actions
(
    {
        name inletFaces;
        type faceSet;
        action new;
        source patchToFace;
        sourceInfo
        {
            name inlet;
        }
    }

    {
        name outletFaces;
        type faceSet;
        action new;
        source patchToFace;
        sourceInfo
        {
            name outlet;
        }
    }
);
