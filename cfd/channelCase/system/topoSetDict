FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}

actions
(
    {
        name inletFaces;
        type faceSet;
        action new;
        source patchToFace;
        sourceInfo
        {
            name channels;
        }
    }

    {
        name inletFaces;
        type faceSet;
        action subset;
        source boxToFace;
        sourceInfo
        {
            box (-0.03 -0.005 -0.001) (-0.02 0.005 0.005);
        }
    }

    {
        name outletFaces;
        type faceSet;
        action new;
        source patchToFace;
        sourceInfo
        {
            name channels;
        }
    }

    {
        name outletFaces;
        type faceSet;
        action subset;
        source boxToFace;
        sourceInfo
        {
            box (0.02 -0.005 -0.001) (0.03 0.005 0.005);
        }
    }
);
